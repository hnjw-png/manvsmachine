//start of code, players button choice.

//player presses rock, paper or scissors button//

for (let button of buttons) {
    button.addEventListener(onclick, function);
    if {
        document.getMyElementById("rock", onclick)
    }
    if {
        button.addEventistenerById("paper",onclick)

    }
    if {
        button.addEventistenerById("scissors", onclick)
    }
}
